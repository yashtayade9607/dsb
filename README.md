# dsb

password : cloudera
mount -t vboxsf shared shared


hbase start commands
sudo service hbase-master start

sudo service hbase-regionserver start

hbase shell


half date command

IF DATEPART('month',[InvoiceDate]) <= 6 THEN "H1"
ELSE "H2"
END
